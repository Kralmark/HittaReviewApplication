package com.example.hittareviewapplication.ui.types

import android.graphics.Color

enum class ColorType(val value: Int) {
    WARNING(Color.YELLOW),
    FAILURE(Color.RED),
    SUCCESS(Color.GREEN)
}